# Copy to .env for local Docker Compose. Set a random secret of at least 32 characters.
LLAMA_API_KEY=
CTX_SIZE=8192

# Optional read token for Hugging Face. Public model works without it.
HF_TOKEN=
